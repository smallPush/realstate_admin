[values]
    phpDocumentRoot = "public"

[phases.setup]
    nixPkgs = ["php83", "php83Extensions.pdo_pgsql", "php83Extensions.intl", "php83Extensions.ctype", "php83Extensions.iconv", "php83Packages.composer", "nginx"]

[phases.install]
    cmds = ["composer install --no-dev --no-interaction --optimize-autoloader"]
